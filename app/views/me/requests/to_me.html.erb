<% @title_box = capture do -%>
  <h1><%= "My requests"[:my_requests_headline] %></h1>
  <table class='info_box my_tasks'><tr>
   <td><%= request_state_links %></td>
   <td class='right'><%= request_source_links %></td>
  </tr></table>
<% end -%>

<%= render :partial => 'requests/table', :locals => {:requests => @requests, :approvable => true} %>

<%= pagination_links(@requests) %>


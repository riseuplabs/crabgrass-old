if @unread_count > 0
  page.replace_html 'pages_tab',
   tab_li :pages, :target => my_work_me_pages_path, :current => (@second_nav == 'pages')
     :after => " (#{@unread_cont})"
end

if @to_me_count > 0
  page.replace_html 'requests_tab',
    tab_li :requests, :after => "#{@to_me_count}"
end

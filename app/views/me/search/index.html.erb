<% @title_box = content_tag :h1, "Search"[:my_search_headline] %>
<% @info_box = "Search through all pages you have access to."[:me_search_description] %>

<%= render :partial => 'pages/search', :locals => {:users => @user.contacts, :groups => @user.groups} %>

<br/>

<%= render :partial => 'pages/list', :locals => {:heading => true} %>


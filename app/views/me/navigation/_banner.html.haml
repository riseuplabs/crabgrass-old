%div.left
  %h1= current_user.display_name
  = render :partial => 'common/say_form', :locals => {:submit => I18n.t(:say_message_button), :url => '/me/public_messages', :text => current_user.current_status}
  %a{:href => url_for_user(current_user)}
    View Profile
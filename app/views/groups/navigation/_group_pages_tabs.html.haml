%ul
  = menu I18n.t(:all_pages_tab), [group_search_url(:action => 'pages'), group_search_url(:action => 'archive')]
  - if may_contributions_group?
    = menu I18n.t(:contributions), group_search_url(:action => 'contributions')
  - if may_trash_group?
    = menu I18n.t(:trash), group_search_url(:action => 'trash')


<%= link_to 'New group', :action => 'new' %>

<h1>My groups</h1>

<% current_user.groups.each do |g| %>
<%= link_to g.name, :controller => 'groups', :action => 'view', :id => g %>
<% end %>

<h1>Groups by topic</h1>

(not yet working)

<h1>Groups by region</h1>

(not yet working)

<h1>Search</h1>

<% for group in @groups %>
<%= link_to group.name, :action => 'view', :id => group %>
<% end %>


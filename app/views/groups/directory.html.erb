<% @title_box = content_tag :h1, 'Group Directory'[:group_directory] -%>

<%= letter_pagination_links({:controller => 'groups', :action => 'directory'}, :available_letters => @pagination_letters) %>

<%= render :partial => 'group', :collection => @groups %>
<%= pagination_links(@groups) %>


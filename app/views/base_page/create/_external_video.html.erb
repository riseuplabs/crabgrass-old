<%
  form.row do |r|
    r.label 'Embed'[:embed]
    r.input text_area('external_video', 'media_embed', :size => '40x3')
    r.info 'Embed code from external video source'[:video_embed_code_prompt] + " (" + 'currently supported: youtube, blip.tv, google video, vimeo'[:current_video_sources] + ")"
  end
%>


Hello <%= @user.display_name %>,

The page "<%= @page_history.page.title %>" in network "<%= @site.title %>" has been modified:

  <%= description_for(@page_history) %>

You may access this page here:

  <%= "http://#{@site.domain}#{page_url(@page_history.page)}" %>

To stop watching this page for changes, click here:

  <%= "http://#{@site.domain}#{page_url(@page_history.page)}" %>

---
Change your email notification settings:
<%= "http://#{@site.domain}#{me_url(:action => 'edit')}" %>

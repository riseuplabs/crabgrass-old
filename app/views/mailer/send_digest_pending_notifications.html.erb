Hello <%= @user.display_name %>,

The page "<%= @page.title %>" in network "<%= @site.title %>" has been modified,
this is a digest email resuming the last changes:

<% @page_histories.each do |page_history| %>
<%= description_for(page_history) %>
-----------------------------------------------------------------------------

<% end %>

You may access this page here:

  <%= page_url(@page) %>

To stop watching this page for changes, click here:

  <%= page_url(@page) %>

---
Change your email notification settings:
<%= me_url(:action => 'edit') %>

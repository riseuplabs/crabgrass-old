%li[widget]{:class => ('small_widget' if widget.small?)}
  .left
    %h4
      = preview_widget_link(widget)
    = widget.name.sub(/Widget$/, '') unless widget.title.blank?
  %span
    = edit_widget_link(widget)

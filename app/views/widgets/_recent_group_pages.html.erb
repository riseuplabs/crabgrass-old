<%= header_with_more(:h2, :alert, I18n.t(:recent_member_group_pages, :network => @group.full_name)) %>
<%= render :partial => 'pages/list', :locals => {:pages => @group_pages, :columns => [:stars, :owner, :icon, :title, :last_updated], :sortable => false} %>
<%# link_to I18n.t(:see_more_link) + ARROW, group_search_url('descending', 'updated_at') %>

<p>
<i>Sorry, page <b><%= h params[:_page]%></b> does not exist.</i>
<ul>
  <% if logged_in? and @group and current_user.member_of?(@group) %>
  <li><%= link_to "create #{h params[:_page]}", {:controller => 'pages', :action => :create_wiki, :name => params[:_page], :group => @group.name }, :method=>:post %></li>
  <% end %>
  <% if request.env["HTTP_REFERER"] %>
  <li><%= "<a href='%s'>return to previous page</a>" % request.env["HTTP_REFERER"] %></li>
  <% end %>
</ul>
</p>

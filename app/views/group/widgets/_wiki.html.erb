<%# this is a widget like _group_wiki, but more generic. It can be used to show any particular wiki object. %>
<% wiki = Wiki.new if wiki.nil? %>
<% editable = false if editable.nil? %>

<% if editable -%>
  <%= render :partial => 'wiki/show', :locals => {:wiki => wiki} %>
<% else %>
  <div class='wiki'>
    <%= wiki.body_html %>
  </div>
<% end -%>


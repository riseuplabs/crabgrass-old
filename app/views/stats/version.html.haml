-content_for :banner do
  = content_tag(:h1, 'Statistics')
= render :partial => 'tabs'


%table.pretty(style="width:200px")
  %tbody
  - @version_info.each do |key, value|
    %tr
      %td= key
      %td
        %strong= value

<%
  # hide the flash, so that error messages are not displayed by the layout
  flash[:hide] = true
%>
<% dialog_page(
  :title => content_tag(:strong, flash[:title] || I18n.t(:alert_error), :class => 'error_48 big_icon'), 
  :details => ""
) do %>

  <%= flash[:text] %>

<% end %>




<%=
   if @page.tags.blank?
     "no tags"
   else
     @page.tags.collect{|tag| link_to tag.name, "/me/search/tag/#{tag.name}" }.join(", ")
   end
%>
<%= link_to_function(image_tag('actions/edit.png'), "Element.toggle($('tag-form')); Element.toggle($('tags'))") %>


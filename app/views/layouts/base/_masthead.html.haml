/ %section{:id => 'masthead', :style => section_style}
/   %div{:id => 'masthead-container', :style => "#{style}"}
/     #masthead-name
/       - if render_title
/         = custom_masthead_site_title
/       = render :partial => '/pages/mini_search' 

%header#masthead
  #masthead_content
    = theme_render(:masthead_content_html)
  - if current_theme.masthead_nav_style == 'cutout'
    = render :partial => '/layouts/menu/top_nav'


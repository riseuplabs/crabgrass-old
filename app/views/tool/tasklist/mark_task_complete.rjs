page.remove "task_#{@task.id}"
page.insert_html :bottom, 'completed_tasks', :partial => 'task', :locals => {:task => @task}
page.sortable "completed_tasks", :handle => 'handle', :url => page_url(@page, :action=>'sort')

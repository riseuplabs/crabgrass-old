<%
  detail_view_navigation(@page, @previous, @showing.image, @next)
-%>

<%= gallery_navigation %>

<div class="gallery-item">
  <%= link_to thumbnail_img_tag(@showing.image, 'large'), @image.url_from_gallery(@page.id) %>
  <%= h(@showing.image.caption) %>
</div>

<%= gallery_display_image_position %>


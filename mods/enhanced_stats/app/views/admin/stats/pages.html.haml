= render :partial => 'stats_head', :locals => {:section => 'Page Info'}
%div.stats_container
  %h2 Select a Date Range
  = render :partial => 'select_date_range', :locals => {:controller => '/admin/stats/pages'}
  - if @show_stats
    = render :partial => 'stats_results'

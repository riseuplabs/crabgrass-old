<p>
  <b>Key Name:</b>
  <%=h @key.name %>
</p>

<p>
  <b>Text in <%= Language.default.name %>:</b>
  <%= @key.default %>
</p>

<p>
  <b>Already translated to:</b>
  <% @key.translations.each do |t| %>
    <% outdated = t.out_of_date? ? " (outdated)" : "" %>
    <%= link_to h(t.language.name + outdated), edit_translation_path(t) %>
  <% end %> 
</p>

<p>
  <b>Translate to: </b> 
  <% for language in @languages -%>
    <%= link_to(language.name, :controller => :translations, :action => :new, :key => @key.name, :language => language) unless @key.languages.include?(language) %>
  <% end -%>
</p>

<br/><br/>

<%= link_to 'Edit', edit_key_path(@key) %> |
<%= link_to 'Back', keys_path %>
